/* 폼 유효성 검사 */
// EMAIL

// USERNAME

// PASSWORD
export const PASSWORD_REGEX = new RegExp(
  /^(?=.*[a-z])(?=이미 사용 중인 이메일 입니다..*[A-Z])(?=.*\d)(?=.*?[#?!@$%^&*-]).+$/
);
// TOKEN

/* 폼 에러 메세지 */
// EMAIL

// USERNAME

// PASSWORD

// TOKEN
